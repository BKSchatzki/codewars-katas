// Simple function that takes two number arguments and returns their product

function multiply(a, b){
  let product = a * b;
  return product;
}

// I could have:
  // Returned directly without creating variable.
  // Added a check for non-number arguments.
